services:
  manasim:
    image: ghcr.io/hakamana123/manasim:latest
    container_name: manasim
    env_file:
      - .env
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
